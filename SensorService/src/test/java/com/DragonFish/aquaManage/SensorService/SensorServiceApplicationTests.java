package com.DragonFish.aquaManage.SensorService;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Unit test for ensuring that the Spring application context
 * for the SensorService loads without issues.
 */
@SpringBootTest
class SensorServiceApplicationTests {

	/**
	 * Test to check if the application context loads correctly.
	 */
	@Test
	void contextLoads() {
		// This test will pass if the application context loads successfully.
		// No additional assertions are required.
	}
}
